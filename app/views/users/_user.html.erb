<tr>
	<td><%= link_to user.username, user %></td>
	<td><%= user.email %></td>
	<% if current_user.admin? && !current_user?(user) %>
		<td><%= link_to "Delete", user, method: "delete", 
				data: {confirm: "Are you sure you wish to delete the user?"} %></td>
	<% end %>
</tr>